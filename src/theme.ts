import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ff8800', // Burayı istediğiniz renge değiştirin
      // ...diğer renkler...
    },
    // ...diğer paletler...
  },
  // ...diğer tema ayarları...
});

export default theme;